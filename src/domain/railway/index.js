class Railway {
  construct({ name, description='', destination, trigger, bullet }) {
    this.name = name
    this.description = description
    this.destination = destination
    this.trigger = trigger
    this.bullet = bullet
  }
}