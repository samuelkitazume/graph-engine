class Station {
  constructor ({ name, description='', railways, initial=false }) {
    this.name = name
    this.description = description
    this.railways = railways
    this.initial = initial
  }
}